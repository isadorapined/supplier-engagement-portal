node_modules
dist
.DS_Store
*.local

# Supabase credentials. `*.local` already covers .env.local, but a plain .env
# would otherwise be committed — and from v3.0 this project has keys to leak.
.env
.env.*
!.env.example

tests/fixtures/scorecard.pdf
